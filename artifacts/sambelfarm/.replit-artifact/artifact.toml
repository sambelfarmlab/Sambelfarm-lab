kind = "web"
previewPath = "/"
title = "Sambelfarm Content Lab"
version = "1.0.0"
id = "artifacts/sambelfarm"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24842

[services.development]
run = "pnpm --filter @workspace/sambelfarm run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/sambelfarm", "run", "build" ]
publicDir = "artifacts/sambelfarm/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24842"
BASE_PATH = "/"
